language: python
dist: trusty
sudo: false

python:
  - "3.6"
  - "nightly"

install:
  - ./setup.py develop

script:
  - cd tests
  - pytest --verbose

notifications:
  email:
    on_success: change
    on_failure: change
  irc:
    channels:
      - "irc.rizon.net#raylu"
    skip_join: true
    on_success: change
    on_failure: always
    template:
      - "%{repository}#%{build_number} (%{branch} - %{commit} : %{author}): %{message} %{build_url}"
